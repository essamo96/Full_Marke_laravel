/**
 * Student dashboard extras (modals / page helpers live elsewhere).
 * Sidebar behavior is in assets/js/dashboard-sidebar.js
 */
document.addEventListener('DOMContentLoaded', function () {
  // Reserved for student-dashboard-only widgets.
});
