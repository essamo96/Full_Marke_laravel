# 🎓 نظام إدارة أكاديمية الدروس الخصوصية
## تحليل شامل للنظام — Full System Analysis

> **الإصدار:** 1.0 | **تاريخ الإنشاء:** 2026 | **اللغة الأساسية:** العربية (RTL)

---

## 📋 الفهرس

1. [نظرة عامة على النظام](#1-نظرة-عامة)
2. [التسلسل الهرمي للنظام](#2-التسلسل-الهرمي)
3. [خريطة الشاشات التفصيلية](#3-خريطة-الشاشات)
4. [مخطط قاعدة البيانات](#4-قاعدة-البيانات)
5. [تحليل النظام المالي](#5-النظام-المالي)
6. [تدفقات العمل](#6-تدفقات-العمل)
7. [متطلبات UI/UX](#7-متطلبات-ux)
8. [البرومت الاحترافي لبناء النظام](#8-البرومت)

---

## 1. نظرة عامة

نظام ويب متكامل لإدارة أكاديمية تعليمية متخصصة في الدروس الخصوصية، يربط بين:
- **المركز التعليمي** (الإدارة والأدمن)
- **المدرسين** (إدارة المجموعات والمحتوى)
- **الطلاب** (التسجيل والدراسة)
- **أولياء الأمور** (لبرامج الأطفال)

### أنواع المستخدمين

| المستخدم | الصلاحيات |
|-----------|-----------|
| **زائر عام** | تصفح البرامج، قراءة التفاصيل، طلب التسجيل |
| **طالب مفعّل** | إضافة مواد للسلة، الدفع، مشاهدة الموارد |
| **ولي أمر** | نفس صلاحيات الطالب لحساب طفله |
| **أدمن** | إدارة كاملة للنظام والمدفوعات والمحتوى |
| **محاسب** | صلاحيات مالية فقط |

---

## 2. التسلسل الهرمي

```
الأكاديمية
│
├── برنامج التوجيهي
│   ├── مادة الرياضيات
│   │   ├── مجموعة (أ) — السبت والاثنين 5-7م
│   │   └── مجموعة (ب) — الأحد والثلاثاء 3-5م
│   ├── مادة الفيزياء
│   └── مادة الكيمياء
│
├── برنامج الأطفال
│   ├── مادة القراءة والكتابة
│   └── مادة الرياضيات التمهيدية
│
└── برنامج التأهيل
    ├── مادة اللغة العربية
    └── مادة اللغة الإنجليزية
```

---

## 3. خريطة الشاشات

### 🌐 أولاً: الشاشات العامة (Public Screens)

---

#### **P1 — الصفحة الرئيسية (Home)**

```
┌─────────────────────────────────────────────────────┐
│  LOGO        [الرئيسية] [البرامج] [تواصل]  [دخول]  │
├─────────────────────────────────────────────────────┤
│                   HERO SECTION                      │
│          "أكاديمية [اسم المركز] للتعليم"           │
│              [ابدأ رحلتك التعليمية]                 │
├─────────────────────────────────────────────────────┤
│  🎓 برنامج التوجيهي │ 🧒 برنامج الأطفال │ 📘 التأهيل│
│  [بطاقة: صورة + اسم + وصف قصير + "التفاصيل"]       │
├─────────────────────────────────────────────────────┤
│                  لماذا نحن؟ (ميزات)                 │
│                      Footer                         │
└─────────────────────────────────────────────────────┘
```

**محتوى كل بطاقة برنامج:**
- صورة البرنامج
- اسم البرنامج
- الوصف القصير (حتى 100 حرف)
- عدد المواد المتاحة
- زر "عرض التفاصيل"

---

#### **P2 — تفاصيل البرنامج (Program Details)**

```
┌─────────────────────────────────────────────────────┐
│  [صورة البرنامج الكاملة]                           │
│  اسم البرنامج                                       │
│  الوصف التفصيلي الكامل                              │
├─────────────────────────────────────────────────────┤
│  📚 المواد المتاحة                                  │
│  ┌────────────────────────────────────────────────┐ │
│  │ مادة الرياضيات  │ رسوم: 200₪ │ حد أدنى: 100₪ │ │
│  │ المدرس: أ.محمد  │ المجموعات: 3          [+سلة]│ │
│  └────────────────────────────────────────────────┘ │
│  [للغير مسجل: زر "سجّل الآن" بارز]                 │
└─────────────────────────────────────────────────────┘
```

**بيانات كل مادة تُعرض:**
- اسم المادة + وصف مختصر
- الرسوم الكاملة والحد الأدنى للدفع
- أسماء المدرسين المعيّنين
- عدد المجموعات المتاحة
- زر "أضف للسلة" (يظهر فقط للطالب المسجل)
- زر "سجّل الآن" (يظهر للزوار)

---

#### **P3 — شاشة التسجيل / Apply Now**

**نموذجان بحسب نوع البرنامج:**

**نموذج الطالب العادي:**
```
الاسم الكامل *           | رقم الهوية *
تاريخ الميلاد *          | الجنس (ذكر/أنثى) *
رقم الهاتف *             | البريد الإلكتروني *
العنوان *                |
──────────────────────────
كلمة المرور *            | تأكيد كلمة المرور *
الصورة الشخصية (اختياري) |
──────────────────────────
[  سجّل وأكمل التحقق  ]
```

**نموذج برنامج الأطفال (قسمان):**
```
── بيانات الطفل ──────────────────────────────────────
اسم الطفل *              | تاريخ الميلاد *
الجنس *                  |

── بيانات ولي الأمر (الحساب) ─────────────────────────
الاسم الكامل *           | رقم الهوية *
العلاقة بالطالب *         | رقم الهاتف *
البريد الإلكتروني *      | العنوان *
──────────────────────────
كلمة المرور *            | تأكيد كلمة المرور *
──────────────────────────
[  سجّل وأكمل التحقق  ]
```

> **ملاحظة:** البرنامج المحدد مسبقاً يُمرر تلقائياً في الطلب.

---

#### **P4 — التحقق من البريد الإلكتروني**

```
┌─────────────────────────────────────────────┐
│  📧 تم إرسال رمز التحقق                    │
│  إلى البريد: user@example.com               │
│                                             │
│  [ _ ] [ _ ] [ _ ] [ _ ] [ _ ] [ _ ]       │
│         (أدخل الرمز المكوّن من 6 أرقام)   │
│                                             │
│  [    تحقق من حسابي    ]                   │
│                                             │
│  لم تستلم الرمز؟ [إعادة الإرسال 00:59]    │
└─────────────────────────────────────────────┘
```

---

#### **P5 — تسجيل الدخول (Login)**

```
البريد الإلكتروني أو رقم الهاتف
كلمة المرور
[  تسجيل الدخول  ]
نسيت كلمة المرور؟ | ليس لديك حساب؟ سجّل الآن
```

---

### 👨‍🎓 ثانياً: شاشات الطالب (Student Screens)

*تظهر صورة الملف الشخصي في الـ Navbar بعد التفعيل*

---

#### **S1 — لوحة التحكم (Dashboard)**

```
┌─────────────────────────────────────────────────────┐
│  مرحباً، [اسم الطالب] 👋        [صورة] ▼           │
├────────────┬────────────┬────────────┬──────────────┤
│ التسجيلات │  الطلبات  │  المدفوع  │  المتبقي     │
│     3      │     1      │  350₪      │  150₪        │
├─────────────────────────────────────────────────────┤
│  آخر الطلبات                                        │
│  ┌──────────────────────────────────────────────┐   │
│  │ رياضيات — توجيهي   │ معلق ⏳  │ 100₪ مدفوع │   │
│  │ إنجليزي — توجيهي   │ نشط ✅   │ 200₪ مدفوع │   │
│  └──────────────────────────────────────────────┘   │
│  [عرض كل الطلبات]                                   │
└─────────────────────────────────────────────────────┘
```

---

#### **S2 — السلة (Cart)**

```
┌─────────────────────────────────────────────────────┐
│  🛒 سلة التسجيل                                    │
├──────────────────────────────────┬──────────────────┤
│ المادة / البرنامج                │ الرسوم           │
├──────────────────────────────────┼──────────────────┤
│ رياضيات — برنامج التوجيهي       │                  │
│ المجموعة: [اختر ▼]              │ 200₪ (حد أدنى100)│
│                                  │ [🗑 حذف]         │
├──────────────────────────────────┼──────────────────┤
│ فيزياء — برنامج التوجيهي        │                  │
│ المجموعة: مجموعة أ - السبت 5م   │ 180₪ (حد أدنى 90)│
│                                  │ [🗑 حذف]         │
├──────────────────────────────────┼──────────────────┤
│ الإجمالي الكامل                  │ 380₪             │
│ الحد الأدنى المطلوب              │ 190₪             │
└──────────────────────────────────┴──────────────────┘
                          [المتابعة للدفع ←]
```

---

#### **S3 — صفحة الدفع (Payment)**

```
┌─────────────────────────────────────────────────────┐
│  💳 إتمام الدفع                                     │
├─────────────────────────────────────────────────────┤
│  ملخص الطلب:                                        │
│  • رياضيات: 200₪  |  • فيزياء: 180₪               │
│  ─────────────────────────────────────              │
│  الإجمالي: 380₪   | الحد الأدنى: 190₪              │
├─────────────────────────────────────────────────────┤
│  المبلغ المدفوع * [____________] ₪                  │
│  ⚠️ يجب ألا يقل عن 190₪                            │
│                                                     │
│  طريقة الدفع * [تحويل بنكي          ▼]             │
│  ─────────────────────────────────────              │
│  [معلومات الحساب البنكي تظهر هنا]                  │
│                                                     │
│  إشعار الدفع * [📎 رفع الإشعار]                    │
│  (PNG, JPG, PDF — حتى 5MB)                         │
│                                                     │
│  ملاحظات (اختياري) [___________________]            │
├─────────────────────────────────────────────────────┤
│            [  تأكيد وإرسال الطلب  ]                │
└─────────────────────────────────────────────────────┘
```

---

#### **S4 — طلباتي (My Registrations)**

```
تبويبات: [كل الطلبات] [نشط ✅] [معلق ⏳] [مرفوض ❌]

┌─────┬──────────────┬────────┬────────┬────────┬──────────────┐
│ رقم │ المادة       │ الكلي  │ المدفوع│ المتبقي│ الحالة       │
├─────┼──────────────┼────────┼────────┼────────┼──────────────┤
│ 001 │ رياضيات/توجيهي│ 200₪  │ 100₪  │ 100₪  │ نشط ✅       │
│     │              │        │        │        │ [تفاصيل][سداد]│
├─────┼──────────────┼────────┼────────┼────────┼──────────────┤
│ 002 │ فيزياء/توجيهي│ 180₪  │ 0      │ 180₪  │ معلق ⏳      │
│     │              │        │        │        │ [تفاصيل]     │
└─────┴──────────────┴────────┴────────┴────────┴──────────────┘
```

---

#### **S5 — تفاصيل التسجيل**

- بيانات المادة والمجموعة
- جدول سجل المدفوعات (رقم الدفعة / التاريخ / المبلغ / الحالة)
- زر "سداد الباقي"
- **الموارد التعليمية (تظهر فقط بعد تأكيد الدفع):**
  - فيديوهات الدروس
  - مستندات ومواد
  - رابط Zoom للمجموعة

---

#### **S6 — الملف الشخصي (Profile)**

```
[صورة شخصية] [تغيير الصورة]
الاسم: محمد أحمد           | البريد: user@email.com
الهاتف: 059xxxxxxx        | تاريخ التسجيل: 01/01/2025
──────────────────────────────────────────────────
تغيير كلمة المرور:
كلمة المرور الحالية     [_______________]
كلمة المرور الجديدة     [_______________]
تأكيد كلمة المرور الجديدة [_______________]
[حفظ التغييرات]
```

---

### 🛠 ثالثاً: شاشات الأدمن (Admin Screens)

---

#### **A1 — لوحة تحكم الأدمن**

```
┌──────────┬──────────┬──────────┬──────────┐
│  الطلاب  │طلبات معلقة│ إيرادات │ متأخرات  │
│    120   │    8    │ 25,400₪  │  3,200₪  │
└──────────┴──────────┴──────────┴──────────┘
رسم بياني: التسجيلات الشهرية | الإيرادات الشهرية
آخر الطلبات الواردة (جدول مختصر)
```

---

#### **A2 — إدارة البرامج**

**قائمة البرامج:**

| الصورة | اسم البرنامج | عدد المواد | الحالة | إجراءات |
|--------|--------------|------------|--------|----------|
| 🖼 | توجيهي | 8 | نشط ✅ | [تعديل][إخفاء][حذف] |
| 🖼 | أطفال | 5 | نشط ✅ | [تعديل][إخفاء][حذف] |

**نموذج إضافة/تعديل برنامج:**
```
اسم البرنامج (عربي) *    | اسم البرنامج (إنجليزي) *
صورة البرنامج *          | نوع البرنامج *
                         | [عادي / أطفال / تأهيلي]
الوصف القصير * (200 حرف) |
الوصف التفصيلي *         | [محرر نصي غني]
ترتيب العرض              | الحالة [نشط/مخفي]
[حفظ]
```

---

#### **A3 — إدارة المواد**

**نموذج إضافة/تعديل مادة:**
```
البرنامج *               | اسم المادة *
الوصف                    |
رسوم الاشتراك * [____]₪  | الحد الأدنى للدفع * [____]₪
المدرسون المعيّنون        | [اختيار متعدد ▼]
ترتيب العرض              | الحالة
```

---

#### **A4 — إدارة المجموعات**

**نموذج إضافة/تعديل مجموعة:**
```
المادة التابعة لها *     | اسم المجموعة *
المدرس المسؤول *         |
أيام الأسبوع *:
  [☑] الأحد  [☑] الاثنين  [☐] الثلاثاء  [☑] الأربعاء ...
وقت البداية *            | وقت النهاية *
السعة القصوى             | تاريخ البداية | تاريخ الانتهاء
رابط Zoom (اختياري)      |
الحالة                   |
```

---

#### **A5 — الطلبات المعلقة**

تبويبات: **[طلبات تسجيل جديدة]** | **[طلبات دفع]**

```
┌─────────────────────────────────────────────────────┐
│ الطالب: محمد أحمد        | التاريخ: 01/05/2025     │
│ المادة: رياضيات — توجيهي  | المبلغ: 100₪            │
│ طريقة الدفع: تحويل بنكي  | [🖼 عرض الإشعار]        │
│─────────────────────────────────────────────────────│
│ [✅ قبول]  [❌ رفض]  [💬 طلب معلومات]               │
└─────────────────────────────────────────────────────┘
```

---

#### **A6 — إدارة المدفوعات**

```
فلترة: [التاريخ من-إلى] [الحالة ▼] [البرنامج ▼] [تصدير Excel]

┌──────┬──────────┬─────────┬─────────┬─────────┬──────────┐
│ رقم  │ الطالب  │ المادة  │  الكلي  │ المدفوع │  الحالة  │
├──────┼──────────┼─────────┼─────────┼─────────┼──────────┤
│ P001 │ محمد     │ رياضيات │  200₪   │  100₪   │ مؤكد ✅  │
│ P002 │ أحمد     │ فيزياء  │  180₪   │  180₪   │ معلق ⏳  │
└──────┴──────────┴─────────┴─────────┴─────────┴──────────┘
```

---

#### **A7 — الموارد التعليمية**

لكل مادة صفحة تحتوي:
```
[+ إضافة مورد جديد]
النوع: [فيديو / مستند / رابط / Zoom]
العنوان: [_______________]
الرابط/الملف: [_______________]
الوصف: [_______________]
الترتيب: [___]
```

---

#### **A8 — التقارير المالية**

```
تبويبات:
[إيرادات] [متأخرات] [طرق الدفع] [تقرير البرامج]

تقرير الإيرادات:
  - إجمالي الإيرادات المؤكدة
  - إجمالي المتأخرات
  - رسم بياني شهري
  - تقسيم حسب البرنامج/المادة
  [📥 تصدير PDF] [📊 تصدير Excel]
```

---

## 4. قاعدة البيانات

### ERD — العلاقات الرئيسية

```
programs ──< subjects ──< groups
                │
        subject_teachers >── teachers
                │
         registrations >── students
                │              │
            payments      guardians
                │
      payment_registrations
```

---

### الجداول التفصيلية

---

#### جدول: `programs` (البرامج)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `name_ar` | VARCHAR(255) | NOT NULL | الاسم بالعربية |
| `name_en` | VARCHAR(255) | NOT NULL | الاسم بالإنجليزية |
| `slug` | VARCHAR(255) | UNIQUE | الرابط المختصر |
| `short_description` | VARCHAR(500) | NULL | الوصف القصير |
| `long_description` | LONGTEXT | NULL | الوصف التفصيلي |
| `image` | VARCHAR(255) | NULL | مسار الصورة |
| `type` | ENUM | NOT NULL | `regular / children / rehabilitation` |
| `is_active` | BOOLEAN | DEFAULT 1 | الحالة |
| `sort_order` | INT | DEFAULT 0 | ترتيب العرض |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

---

#### جدول: `subjects` (المواد)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `program_id` | BIGINT | FK → programs | البرنامج |
| `name_ar` | VARCHAR(255) | NOT NULL | الاسم بالعربية |
| `name_en` | VARCHAR(255) | NULL | الاسم بالإنجليزية |
| `description` | TEXT | NULL | الوصف |
| `fee` | DECIMAL(10,2) | NOT NULL | الرسوم الكاملة |
| `min_payment` | DECIMAL(10,2) | NOT NULL | الحد الأدنى للدفع |
| `is_active` | BOOLEAN | DEFAULT 1 | الحالة |
| `sort_order` | INT | DEFAULT 0 | الترتيب |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

> **قاعدة:** `min_payment ≤ fee`

---

#### جدول: `groups` (المجموعات)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `subject_id` | BIGINT | FK → subjects | المادة |
| `teacher_id` | BIGINT | FK → teachers | المدرس |
| `name` | VARCHAR(255) | NOT NULL | اسم المجموعة |
| `days` | JSON | NOT NULL | أيام الأسبوع `["sat","sun","mon"]` |
| `start_time` | TIME | NOT NULL | وقت البداية |
| `end_time` | TIME | NOT NULL | وقت النهاية |
| `max_capacity` | INT | DEFAULT 30 | السعة القصوى |
| `current_count` | INT | DEFAULT 0 | العدد الحالي |
| `start_date` | DATE | NULL | تاريخ البداية |
| `end_date` | DATE | NULL | تاريخ الانتهاء |
| `zoom_link` | VARCHAR(500) | NULL | رابط Zoom |
| `is_active` | BOOLEAN | DEFAULT 1 | الحالة |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

---

#### جدول: `teachers` (المدرسون)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `name` | VARCHAR(255) | NOT NULL | الاسم الكامل |
| `email` | VARCHAR(255) | UNIQUE, NULL | البريد الإلكتروني |
| `phone` | VARCHAR(20) | NULL | الهاتف |
| `specialty` | VARCHAR(255) | NULL | التخصص |
| `bio` | TEXT | NULL | السيرة الذاتية |
| `photo` | VARCHAR(255) | NULL | الصورة الشخصية |
| `is_active` | BOOLEAN | DEFAULT 1 | الحالة |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

---

#### جدول: `subject_teachers` (ربط المواد بالمدرسين — pivot)

| الحقل | النوع | الوصف |
|-------|-------|-------|
| `subject_id` | BIGINT FK | المادة |
| `teacher_id` | BIGINT FK | المدرس |

---

#### جدول: `students` (الطلاب)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `full_name` | VARCHAR(255) | NOT NULL | الاسم الكامل |
| `national_id` | VARCHAR(20) | UNIQUE, NULL | رقم الهوية |
| `birth_date` | DATE | NULL | تاريخ الميلاد |
| `gender` | ENUM | NULL | `male / female` |
| `phone` | VARCHAR(20) | NULL | الهاتف |
| `email` | VARCHAR(255) | UNIQUE | البريد الإلكتروني |
| `password` | VARCHAR(255) | NOT NULL | كلمة المرور (bcrypt) |
| `address` | TEXT | NULL | العنوان |
| `photo` | VARCHAR(255) | NULL | الصورة الشخصية |
| `is_child` | BOOLEAN | DEFAULT 0 | هل هو طفل |
| `guardian_id` | BIGINT | FK → guardians, NULL | ولي الأمر |
| `email_verified_at` | TIMESTAMP | NULL | تاريخ تأكيد البريد |
| `is_active` | BOOLEAN | DEFAULT 0 | حالة الحساب |
| `remember_token` | VARCHAR(100) | NULL | |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

---

#### جدول: `guardians` (أولياء الأمور)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `full_name` | VARCHAR(255) | NOT NULL | الاسم الكامل |
| `national_id` | VARCHAR(20) | UNIQUE, NULL | رقم الهوية |
| `phone` | VARCHAR(20) | NOT NULL | الهاتف |
| `email` | VARCHAR(255) | UNIQUE | البريد الإلكتروني |
| `password` | VARCHAR(255) | NOT NULL | كلمة المرور (bcrypt) |
| `address` | TEXT | NULL | العنوان |
| `relationship` | ENUM | NOT NULL | `father/mother/brother/sister/other` |
| `photo` | VARCHAR(255) | NULL | الصورة الشخصية |
| `email_verified_at` | TIMESTAMP | NULL | تأكيد البريد |
| `is_active` | BOOLEAN | DEFAULT 1 | الحالة |
| `remember_token` | VARCHAR(100) | NULL | |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

---

#### جدول: `email_verifications` (رموز التحقق)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `email` | VARCHAR(255) | NOT NULL | البريد |
| `code` | VARCHAR(6) | NOT NULL | الرمز المكوّن من 6 أرقام |
| `user_type` | ENUM | NOT NULL | `student / guardian` |
| `expires_at` | TIMESTAMP | NOT NULL | وقت انتهاء الصلاحية |
| `used_at` | TIMESTAMP | NULL | وقت الاستخدام |
| `created_at` | TIMESTAMP | | |

---

#### جدول: `cart_items` (سلة التسوق)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `user_id` | BIGINT | NOT NULL | الطالب أو ولي الأمر |
| `user_type` | ENUM | NOT NULL | `student / guardian` |
| `subject_id` | BIGINT | FK → subjects | المادة |
| `group_id` | BIGINT | FK → groups, NULL | المجموعة المختارة |
| `created_at` | TIMESTAMP | | |

> **فهرس UNIQUE:** `(user_id, user_type, subject_id)` — منع التكرار

---

#### جدول: `registrations` (التسجيلات)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `registration_number` | VARCHAR(20) | UNIQUE | رقم الطلب (REG-YYYYMMDD-XXXX) |
| `student_id` | BIGINT | FK → students | الطالب |
| `subject_id` | BIGINT | FK → subjects | المادة |
| `group_id` | BIGINT | FK → groups, NULL | المجموعة |
| `total_fee` | DECIMAL(10,2) | NOT NULL | الرسوم الكاملة (snapshot) |
| `amount_paid` | DECIMAL(10,2) | DEFAULT 0 | إجمالي المدفوع |
| `remaining_amount` | DECIMAL(10,2) | COMPUTED | `total_fee - amount_paid` |
| `registration_status` | ENUM | NOT NULL | `pending / active / suspended / completed / cancelled` |
| `payment_status` | ENUM | NOT NULL | `unpaid / partially_paid / fully_paid` |
| `activated_at` | TIMESTAMP | NULL | تاريخ التفعيل |
| `admin_notes` | TEXT | NULL | ملاحظات الأدمن |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

> **فهرس UNIQUE:** `(student_id, subject_id)` — طالب لا يسجل في نفس المادة مرتين

---

#### جدول: `payment_methods` (طرق الدفع)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `name_ar` | VARCHAR(255) | NOT NULL | الاسم |
| `name_en` | VARCHAR(255) | NULL | الاسم بالإنجليزية |
| `details` | TEXT | NULL | تفاصيل الحساب/التعليمات |
| `is_active` | BOOLEAN | DEFAULT 1 | الحالة |
| `sort_order` | INT | DEFAULT 0 | الترتيب |

---

#### جدول: `payments` (الدفعات)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `payment_number` | VARCHAR(20) | UNIQUE | رقم الدفعة (PAY-YYYYMMDD-XXXX) |
| `student_id` | BIGINT | FK → students | الطالب |
| `payment_method_id` | BIGINT | FK → payment_methods | طريقة الدفع |
| `total_amount` | DECIMAL(10,2) | NOT NULL | المبلغ الإجمالي للدفعة |
| `receipt_image` | VARCHAR(255) | NOT NULL | مسار إشعار الدفع |
| `notes` | TEXT | NULL | ملاحظات الطالب |
| `status` | ENUM | DEFAULT pending | `pending / confirmed / rejected` |
| `confirmed_by` | BIGINT | FK → admins, NULL | الأدمن المؤكد |
| `confirmed_at` | TIMESTAMP | NULL | تاريخ التأكيد |
| `rejection_reason` | TEXT | NULL | سبب الرفض |
| `admin_notes` | TEXT | NULL | ملاحظات الأدمن |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

---

#### جدول: `payment_items` (تفاصيل الدفعة — توزيع المبلغ)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `payment_id` | BIGINT | FK → payments | الدفعة |
| `registration_id` | BIGINT | FK → registrations | التسجيل |
| `allocated_amount` | DECIMAL(10,2) | NOT NULL | المبلغ المخصص لهذا التسجيل |
| `created_at` | TIMESTAMP | | |

---

#### جدول: `subject_resources` (الموارد التعليمية)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `subject_id` | BIGINT | FK → subjects | المادة |
| `title` | VARCHAR(255) | NOT NULL | العنوان |
| `type` | ENUM | NOT NULL | `video / document / link / zoom` |
| `url` | VARCHAR(500) | NOT NULL | الرابط أو مسار الملف |
| `description` | TEXT | NULL | الوصف |
| `is_active` | BOOLEAN | DEFAULT 1 | الحالة |
| `sort_order` | INT | DEFAULT 0 | الترتيب |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

---

#### جدول: `admins` (المشرفون)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `name` | VARCHAR(255) | NOT NULL | الاسم |
| `email` | VARCHAR(255) | UNIQUE | البريد |
| `password` | VARCHAR(255) | NOT NULL | كلمة المرور |
| `role` | ENUM | NOT NULL | `super_admin / admin / accountant` |
| `is_active` | BOOLEAN | DEFAULT 1 | الحالة |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |

---

#### جدول: `notifications` (الإشعارات)

| الحقل | النوع | القيود | الوصف |
|-------|-------|--------|-------|
| `id` | BIGINT | PK, AUTO | المعرف |
| `notifiable_id` | BIGINT | NOT NULL | المستلم |
| `notifiable_type` | VARCHAR | NOT NULL | `Student / Guardian / Admin` |
| `type` | VARCHAR(255) | NOT NULL | نوع الإشعار |
| `data` | JSON | NOT NULL | بيانات الإشعار |
| `read_at` | TIMESTAMP | NULL | وقت القراءة |
| `created_at` | TIMESTAMP | | |

---

## 5. النظام المالي

### 5.1 هيكل الرسوم وقواعده

```
لكل مادة:
  fee         = الرسوم الكاملة (مثال: 200₪)
  min_payment = الحد الأدنى المقبول للتسجيل (مثال: 100₪)
  
القاعدة الأساسية: min_payment ≤ fee
```

### 5.2 منطق توزيع المدفوعات

عند دفع مبلغ واحد لمواد متعددة، يتم التوزيع بالتناسب:

```
مثال: الطالب يسجل في مادتين ويدفع 200₪

  مادة أ: fee=200₪, min=100₪
  مادة ب: fee=300₪, min=150₪
  ─────────────────────────────
  إجمالي: 500₪ | الحد الأدنى: 250₪

  ❌ الطالب دفع 200₪ → مرفوض (أقل من 250₪)
  ✅ الطالب دفع 300₪:
     مادة أ تحصل: (200/500) × 300 = 120₪
     مادة ب تحصل: (300/500) × 300 = 180₪
```

### 5.3 حالات الدفع والتسجيل

```
دورة حياة طلب التسجيل:

CART_ITEM → [checkout] → REGISTRATION(pending) + PAYMENT(pending)
                                    ↓
                          Admin يراجع الإشعار
                         ┌────────────────────┐
                         ↓                    ↓
                   confirmed              rejected
                         ↓                    ↓
              REGISTRATION(active)   REGISTRATION(pending)
              amount_paid يُحدّث     + إشعار للطالب بالسبب
```

| حالة الدفع | شرط التفعيل | الوصول للموارد |
|------------|-------------|---------------|
| `unpaid` | — | ❌ |
| `partially_paid` | مؤكد وـ≥ min_payment | ✅ |
| `fully_paid` | مؤكد وـ= fee | ✅ |

### 5.4 سيناريوهات السداد

**سيناريو 1 — الدفع الكامل دفعةً واحدة:**
```
fee=200₪ | min=100₪ | paid=200₪ | remaining=0₪
status: fully_paid ✅
```

**سيناريو 2 — الدفع الجزئي (عند الحد الأدنى):**
```
fee=200₪ | min=100₪ | paid=100₪ | remaining=100₪
status: partially_paid ⚡ → يمكن الوصول للموارد
```

**سيناريو 3 — سداد الباقي لاحقاً:**
```
دفعة 1: 100₪ → amount_paid=100₪, remaining=100₪ (partially_paid)
دفعة 2: 100₪ → amount_paid=200₪, remaining=0₪ (fully_paid)
```

**سيناريو 4 — الرفض وإعادة التقديم:**
```
PAYMENT(pending) → rejected بسبب إشعار غير واضح
→ REGISTRATION تبقى pending
→ الطالب يعيد رفع الإشعار → PAYMENT جديد
```

### 5.5 قواعد التحقق المالي (Validations)

```
عند الدفع:
  1. paid_amount >= sum(min_payment for each subject in cart)
  2. paid_amount <= sum(fee for each subject in cart) [لا يدفع أكثر من الكلي]
  3. subject must not already be registered (active) للطالب نفسه
  4. group must have available capacity (current_count < max_capacity)
  5. receipt_image is required
  6. payment_method must be active
```

### 5.6 التقارير المالية المطلوبة

| التقرير | الوصف | الفترة |
|---------|-------|--------|
| إيرادات مؤكدة | مجموع المبالغ المؤكدة | يومي/شهري/سنوي |
| متأخرات | مجموع الأرصدة المتبقية | لحظي |
| إيرادات حسب البرنامج | توزيع الإيرادات | شهري |
| إيرادات حسب المادة | أكثر المواد إيراداً | شهري |
| طرق الدفع | توزيع حسب الطريقة | شهري |
| الطلاب المديونون | قائمة من لديهم رصيد متبقي | لحظي |

---

## 6. تدفقات العمل

### Flow 1: تسجيل طالب جديد

```
[الزائر]
  → يتصفح البرامج
  → يختار برنامجاً
  → يضغط "سجّل الآن"
  → [P3] يملأ نموذج التسجيل
       ↓
  يُرسل رمز تحقق (6 أرقام) للبريد [صالح 10 دقائق]
       ↓
  [P4] يُدخل رمز التحقق
       ↓
  ✅ تفعيل الحساب:
     - email_verified_at = NOW()
     - is_active = true
     - ظهور الصورة في Navbar
       ↓
  يُوجَّه للصفحة الرئيسية / السلة
```

### Flow 2: تسجيل برنامج أطفال

```
[الزائر]
  → يختار برنامج الأطفال (type = 'children')
  → يضغط "سجّل الآن"
  → [P3-children] يملأ:
       • بيانات الطفل (student)
       • بيانات ولي الأمر (guardian) — هو الحساب الرئيسي
       ↓
  رمز تحقق لبريد ولي الأمر
       ↓
  تفعيل → ولي الأمر يسجل الدخول
       ↓
  يختار مواد للطفل (student_id يُخزَّن في السلة)
       ↓
  يكمل الدفع باسم الطفل
```

### Flow 3: التسجيل في المواد والدفع

```
[الطالب المفعّل]
  → يتصفح البرامج/المواد
  → يضغط [+ أضف للسلة]  → cart_items.insert
  → يضغط [🛒 السلة]
  → يختار مجموعة لكل مادة (مطلوب)
  → [S3] صفحة الدفع:
      • يرى: إجمالي الرسوم + الحد الأدنى
      • يُدخل: المبلغ المدفوع
      • يختار: طريقة الدفع
      • يرفع: إشعار الدفع
      ↓
  submit → يُنشئ:
    - registrations (pending) لكل مادة
    - payment (pending) واحدة
    - payment_items (توزيع المبلغ)
    - cart_items.delete
      ↓
  [الأدمن يُراجع]
  → يُقرأ إشعار الدفع
       ↓ مقبول              ↓ مرفوض
  payment → confirmed    payment → rejected
  registrations → active  إشعار بسبب الرفض
  amount_paid يُحدَّث     registration تبقى pending
  إشعار للطالب           الطالب يعيد التقديم
```

### Flow 4: سداد الرصيد المتبقي

```
[الطالب]
  → [S4] طلباتي
  → يختار تسجيلاً فيه رصيد متبقٍ
  → يضغط [سداد الباقي]
  → يُدخل المبلغ الإضافي + يرفع إشعاراً جديداً
  → payment جديدة (pending)
  → الأدمن يؤكد → تُحدَّث amount_paid للتسجيل
  → إذا amount_paid = total_fee → payment_status = fully_paid
```

---

## 7. متطلبات UX/UI

### الاتجاه والعرض
- RTL بالكامل (عربي أساسي)
- Responsive: Mobile-first
- Dark/Light mode (اختياري مستقبلاً)

### مكونات UI المطلوبة

| المكوّن | الاستخدام |
|---------|-----------|
| Toast Notifications | نجاح / خطأ / تحذير |
| Skeleton Loading | تحميل الصفحات والجداول |
| Confirmation Modal | الحذف والإجراءات الحساسة |
| Badge Status | حالة الدفع / التسجيل |
| Progress Bar | حالة السداد (المدفوع / الكلي) |
| File Uploader | رفع الإشعارات والصور |
| OTP Input | رمز التحقق (6 خلايا) |
| Pagination | الجداول الطويلة |

### الألوان الموصى بها

```css
--primary:    #1e40af;  /* أزرق أكاديمي */
--success:    #10b981;  /* أخضر التأكيد */
--warning:    #f59e0b;  /* برتقالي المعلق */
--danger:     #ef4444;  /* أحمر الرفض */
--background: #f8fafc;  /* خلفية فاتحة */
--card:       #ffffff;  /* خلفية البطاقات */
```

---

## 8. البرومت

---

# 🤖 البرومت الاحترافي لبناء النظام

```
أنت مطوّر Full-Stack خبير. مهمتك بناء نظام إدارة أكاديمية دروس خصوصية متكامل، 
باستخدام نفس البنية البرمجية والأنماط المعمارية المستخدمة في النظام الحالي.

═══════════════════════════════════════════════════════════════

## 🏗️ البنية التقنية (اتبع بنية النظام الحالي)

- Backend: Laravel 12 (PHP 8.3+)
- Frontend: Blade + Alpine.js أو Livewire (حسب النظام الحالي)
- Database: MySQL 8+ / PostgreSQL
- Authentication: Laravel Sanctum / Fortify
- Storage: Laravel Storage (local / S3)
- Queue: Laravel Queue (البريد والإشعارات)
- Mail: SMTP (Mailtrap للتطوير)
- CSS: Tailwind CSS
- Icons: Heroicons / Font Awesome

═══════════════════════════════════════════════════════════════

## 📦 الوحدات المطلوبة (Modules)

### الوحدة 1: البرامج والمواد (Programs & Subjects)

اعمل CRUD كامل لـ:
1. programs (البرامج) — مع رفع الصور وتوليد الـ slug
2. subjects (المواد) — مرتبطة بـ program، تحتوي fee + min_payment
3. groups (المجموعات) — مرتبطة بـ subject + teacher، تحتوي أيام + أوقات + رابط Zoom
4. teachers (المدرسون) — مع إمكانية ربطهم بمواد متعددة

قواعد العمل:
- min_payment يجب أن يكون ≤ fee (validation)
- المجموعة لا تقبل تسجيلات جديدة إذا current_count >= max_capacity
- عند حذف برنامج → soft delete أو منع الحذف إذا يوجد تسجيلات

---

### الوحدة 2: المصادقة والحسابات (Auth & Accounts)

اعمل نظام تسجيل ثنائي:

**A. نموذج تسجيل الطالب العادي:**
  - الحقول: full_name, national_id, birth_date, gender, phone, email, password, address, photo
  - بعد التسجيل: أرسل رمز تحقق (6 أرقام عشوائية) للبريد الإلكتروني
  - الرمز صالح 10 دقائق مخزّن في جدول email_verifications
  - بعد تأكيد الرمز: is_active = true, email_verified_at = now()

**B. نموذج تسجيل الطفل (برامج type='children'):**
  - بيانات الطفل: full_name, birth_date, gender (تُحفظ كـ student مع is_child=true)
  - بيانات ولي الأمر: full_name, national_id, phone, email, password, relationship, address
  - الحساب الرئيسي هو ولي الأمر (guardian) — هو من يسجّل الدخول
  - ولي الأمر يستطيع إدارة أكثر من طفل

**التحقق:**
  - شاشة OTP (6 خلايا منفصلة)
  - "إعادة إرسال" مع Countdown 60 ثانية
  - بعد التحقق: redirect للصفحة الرئيسية وتظهر صورة الملف الشخصي في Navbar

**تسجيل الدخول:**
  - بالبريد أو الهاتف + كلمة المرور
  - تذكرني (remember me)
  - نسيت كلمة المرور (email reset link)

---

### الوحدة 3: تجربة الطالب (Student Experience)

**3.1 تصفح البرامج:**
  - صفحة عامة تعرض البرامج كـ Cards
  - صفحة تفاصيل البرنامج: الصورة + الوصف + قائمة المواد
  - لكل مادة: الاسم، الرسوم، الحد الأدنى، المدرسون، المجموعات
  - للزوار: زر "سجّل الآن" يوجّه لصفحة Apply Now
  - للطلاب المسجلين: زر "أضف للسلة" (يتحقق: ليس مسجلاً مسبقاً، ليس موجوداً في السلة)

**3.2 السلة (Cart):**
  - عرض المواد المضافة
  - لكل مادة: اختيار المجموعة (مطلوب) + زر حذف
  - ملخص: إجمالي الرسوم + إجمالي الحد الأدنى
  - السلة تُخزّن في DB (ليس Session) لاستمرارية عبر الجلسات

**3.3 الدفع (Checkout):**
  - ملخص المواد والمبالغ
  - حقل المبلغ المدفوع (validation: >= مجموع min_payment)
  - قائمة طرق الدفع المتاحة (تعرض التعليمات عند الاختيار)
  - رفع إشعار الدفع (PNG/JPG/PDF، حتى 5MB)
  - ملاحظات اختيارية
  - عند Submit:
    → أنشئ payment (status=pending)
    → أنشئ registration لكل مادة (status=pending, payment_status=unpaid)
    → أنشئ payment_items (توزيع المبلغ بالتناسب)
    → احذف cart_items
    → أرسل إشعار بريدي للطالب والأدمن

**3.4 متابعة الطلبات:**
  - جدول التسجيلات مع: حالة التسجيل + حالة الدفع + شريط التقدم
  - صفحة تفاصيل: سجل الدفعات كاملاً
  - زر "سداد الباقي" للتسجيلات التي لها رصيد
  - الموارد التعليمية تظهر فقط عند: registration_status=active

**3.5 الملف الشخصي:**
  - عرض البيانات الشخصية
  - تغيير الصورة (مع crop/resize)
  - تغيير كلمة المرور (تحقق من القديمة أولاً)

---

### الوحدة 4: لوحة تحكم الأدمن (Admin Panel)

**4.1 Dashboard:**
  - KPIs: إجمالي الطلاب، الطلبات المعلقة، الإيرادات المؤكدة، الأرصدة المتبقية
  - رسم بياني: تسجيلات آخر 6 أشهر، إيرادات آخر 6 أشهر
  - جدول: آخر 10 طلبات دفع واردة

**4.2 إدارة البرامج/المواد/المجموعات:**
  - CRUD كامل بالـ validations
  - Soft Delete مع إمكانية الاسترداد
  - Reorder (drag & drop أو sort_order)
  - تفعيل/إخفاء البرامج والمواد

**4.3 الطلبات المعلقة:**
  - تبويبان: [طلبات تسجيل جديدة] [طلبات دفع]
  - عرض: بيانات الطالب + المادة + إشعار الدفع (preview)
  - إجراءات:
    → قبول: 
       - payment.status = confirmed
       - payment.confirmed_by = admin.id
       - payment.confirmed_at = now()
       - registrations ذات الصلة → status=active, payment_status يُحسب
       - amount_paid لكل registration يُحدَّث من payment_items
       - إشعار بريدي للطالب
    → رفض:
       - payment.status = rejected + rejection_reason
       - إشعار بريدي للطالب مع سبب الرفض

**4.4 إدارة المدفوعات:**
  - فلترة متقدمة: التاريخ / الحالة / البرنامج / المادة / الطالب
  - إضافة دفعة يدوية (Admin يضيف دفعة نيابةً عن الطالب)
  - تصدير Excel + PDF
  - عرض ملف الدفع المرفق في Lightbox

**4.5 الموارد التعليمية:**
  - لكل مادة: إدارة قائمة موارد (video/document/link/zoom)
  - رفع الملفات + روابط خارجية
  - إعادة الترتيب

**4.6 التقارير المالية:**
  - تقرير الإيرادات (مع رسوم بيانية)
  - تقرير المتأخرات (الطلاب الذين لديهم رصيد)
  - تقرير طرق الدفع
  - تقرير لكل برنامج/مادة
  - تصدير لكل تقرير

---

## 🔒 قواعد الأمان المطلوبة

1. الصفحات تتطلب المصادقة (middleware: auth)
2. فصل middleware: [web.student], [web.admin], [web.guardian]
3. منع الوصول للموارد التعليمية بدون تسجيل نشط
4. حماية ملفات الإشعارات المرفوعة (private storage)
5. Rate limiting على تسجيل الدخول وإرسال OTP
6. CSRF protection على جميع النماذج
7. Validation قوية على جميع المدخلات

---

## 📧 القوالب البريدية المطلوبة

1. رمز التحقق من البريد الإلكتروني
2. تأكيد استلام طلب الدفع (للطالب)
3. تأكيد طلب دفع جديد (للأدمن)
4. قبول الطلب وتفعيل التسجيل (للطالب)
5. رفض الطلب مع السبب (للطالب)
6. إعادة تعيين كلمة المرور

---

## 🎨 متطلبات UI/UX

- دعم RTL كامل (direction: rtl)
- ألوان: primary=#1e40af, success=#10b981, warning=#f59e0b, danger=#ef4444
- Responsive على جميع الشاشات
- Toast notifications للعمليات
- Loading states وSkeleton screens
- Confirm modals للإجراءات الخطيرة
- Progress bars لحالة السداد
- OTP Input مع 6 خلايا منفصلة

---

## 📊 الأولوية في التنفيذ

### المرحلة 1 (Core):
  - Migrations وModels وRelationships
  - نظام المصادقة الكامل (تسجيل + OTP + تسجيل دخول)
  - CRUD: Programs → Subjects → Groups → Teachers

### المرحلة 2 (Student Flow):
  - صفحة تصفح البرامج العامة
  - السلة والدفع
  - لوحة تحكم الطالب وطلباته

### المرحلة 3 (Admin):
  - لوحة تحكم الأدمن
  - الطلبات المعلقة وتأكيد الدفع
  - إدارة المدفوعات والتقارير

### المرحلة 4 (Enhanced):
  - الموارد التعليمية
  - الإشعارات
  - التقارير المالية

---

## ⚠️ تعليمات مهمة للتنفيذ

1. اتبع نفس هيكل المجلدات والـ naming conventions المستخدمة في النظام الحالي
2. استخدم نفس أسلوب كتابة الـ Controllers (Resource Controllers)
3. الـ Responses يجب أن تكون متسقة (JSON للـ AJAX، redirect للـ forms)
4. كل Model يجب أن يحتوي: $fillable, $casts, relationships
5. استخدم Form Requests لجميع الـ validations
6. استخدم Database Transactions للعمليات المتشعبة (الدفع خصوصاً)
7. أضف Seeders للبيانات الافتراضية (Admin, PaymentMethods)
8. اكتب Factory لكل Model للـ testing
9. كل العمليات المالية تمر عبر DB Transaction لمنع التناقضات
10. الملفات المرفوعة تُخزَّن في storage/private مع signed URLs

═══════════════════════════════════════════════════════════════

ابدأ بالـ Migration files ثم Models ثم Controllers بالترتيب المنطقي.
اسألني إذا احتجت توضيحاً لأي منطق عمل (business logic) قبل البدء.
```

---

*نهاية الوثيقة — Academy System Analysis v1.0*
